{
  "name": "baas",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd backend && uv run uvicorn app.main:app --reload\" \"cd frontend && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "nodemailer": "^8.0.10",
    "pg": "^8.21.0",
    "recharts": "^3.8.1"
  }
}
